<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        <link rel="stylesheet" href="css/forms.css">
        <link rel="shortcut icon" href="img/favicon.png" type="image/x-icon">
        <title>Bed2Transit - Login</title>
        <script>
            function show_password() {
                var input_pass = document.getElementById("password");
                if (input_pass.type === "password") {
                    input_pass.type = "text";
                } else {
                    input_pass.type = "password";
                }
            }
        </script>
    </head>

    <body>
        <!-- Navigation bar -->
        <nav class="navbar navbar-expand-lg color-green">
            <a class="navbar-brand" href="/">Bed2Transit</a>
            <a class="navbar-item" href="/signup">Sign Up</a>
        </nav>

        <div class="island">
            <h2>Login</h2>
            <h4>Don't have an account? <a href="/signup">Make one here.</a></h4>

            <!-- If error, show it. Possible errors: No user with that email or wrong password -->
            <span class="error-color">
                <% if (locals.err_msg) { %>
                    <%= locals.err_msg %>
                <% }%>
            </span>

            <!-- Form to login -->
            <form action="/login" method="POST">
                <label for="email">Email:</label><br>
                <input type="email" id="email" name="email" required><br>

                <label for="password">Password:</label><br>
                <input type="password" id="password" name="password" required><br>
                <input type="checkbox" onclick="show_password()"> Show Password<br><br>

                <button type="submit" class="form-submit">Login</button>
            </form> 

        </div>

    </body>
</html>